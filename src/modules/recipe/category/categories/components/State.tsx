export enum CategoryState {
  NORMAL,
  DELETING,
  UPDATING,
  SUCCESS,
  DRAGGING,
}

export enum CategorySelectedState {
  SELECTED,
  UNSELECTED,
}

export enum CategoryType {
  ADD,
  CATEGORY,
}
